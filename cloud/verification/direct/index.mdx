---
title: "Experimental Direct Model Endpoints"
description: "Experimental verification notes for direct completions endpoints."
---

<Warning>
  Direct model endpoint verification is experimental and is not recommended for new integrations or production verification workflows. Use the [NEAR AI Cloud Gateway](/cloud/verification/cloud-api) instead.
</Warning>

Independent requests to the same direct hostname can reach different serving instances. An attestation report, including `all_attestations[]`, is evidence for the request that returned it; it does not establish deployment evidence for a later completion. A later response-signature request can return `404`, so do not use direct response signatures as a production verification gate.

## Available experimental checks

<CardGroup cols={2}>
  <Card title="Model attestation report" icon="brain-circuit" href="/cloud/verification/direct/model-attestation">
    Inspect the model environment reported by a direct endpoint.
  </Card>
  <Card title="TLS connection binding" icon="shield-check" href="/cloud/verification/direct/tls">
    Bind one live HTTPS connection to a direct report.
  </Card>
  <Card title="Response signatures" icon="message-square-check" href="/cloud/verification/direct/response-signatures">
    Inspect an available signature for exact response bytes.
  </Card>
  <Card title="Image provenance" icon="package-check" href="/cloud/verification/direct/image-provenance">
    Check that the software images in the attested environment meet your source and build policy.
  </Card>
</CardGroup>

## Start with model attestation

1. Generate a fresh nonce for this verification attempt.
2. Request a fresh report from the same direct endpoint your application uses for inference.
3. Verify the model's hardware attestation and apply your acceptance policy.
4. Add TLS, response-signature, or image-provenance checks only for the direct request under inspection.

Start with [Model attestation report](/cloud/verification/direct/model-attestation). Do not combine these experimental checks into a production deployment-verification conclusion for later independent requests.
