---
title: "Choose a TLS Attestation Flow"
description: "Select the TLS verification guide that matches the endpoint your application calls."
---

TLS evidence applies to a particular request path and connection. Select the endpoint your application calls.

<CardGroup cols={2}>
  <Card title="Cloud API TLS binding" icon="cloud" href="/cloud/verification/cloud-api/tls">
    Verify the TLS connection to cloud-api.near.ai against `gateway_attestation`.
  </Card>
  <Card title="Direct endpoint TLS binding" icon="server" href="/cloud/verification/direct/tls">
    Verify the TLS connection to a direct model endpoint against its model report.
  </Card>
</CardGroup>
