---
title: "Verification"
description: "Verify requests sent through the NEAR AI Cloud Gateway."
---

Use these guides to verify the evidence returned for requests sent through the NEAR AI Cloud Gateway.

<Tip>
  See an example implementation in the [NEAR AI Cloud Verification Example](https://github.com/near-examples/nearai-cloud-verification-example) repo. A more complete verifier is available in the [NEAR AI Cloud Verifier](https://github.com/nearai/nearai-cloud-verifier) repo.
</Tip>

## Verify a NEAR AI Cloud Gateway request

<CardGroup cols={1}>
  <Card title="NEAR AI Cloud Gateway" icon="cloud" href="/cloud/verification/cloud-api">
    Use this when your application sends requests through `cloud-api.near.ai`. Verify the gateway and, when your policy requires it, every returned NEAR model-attestation candidate.
  </Card>
</CardGroup>

<Note>
  Using a direct completions endpoint (`{slug}.completions.near.ai`)? Direct verification is experimental. See [Direct Completions](/cloud/experimental/direct-completions) for the current limitations.
</Note>

## What happens next

The Gateway guide walks through verifying fresh deployment evidence before a completion and, when present, a response signature against its reported signer. Depending on your application's policy, you can also verify the TLS connection and software-image provenance.

## Common verification concepts

<CardGroup cols={2}>
  <Card title="Quote, nonce, and signer bindings" icon="fingerprint" href="/cloud/verification/reference/quote-nonce-signer">
    Learn how fresh attestation evidence is tied to your request and to a signing identity.
  </Card>
  <Card title="Verification policy" icon="list-checks" href="/cloud/verification/reference/verification-policy">
    Decide which checks, measurements, and source-provenance requirements your application accepts.
  </Card>
</CardGroup>
