---
title: "Choose an Image-Provenance Flow"
description: "Select the provenance guide that matches the endpoint your application uses."
---

Choose the guide from the URL your application uses for inference.

<CardGroup cols={2}>
  <Card title="NEAR AI Cloud gateway image provenance" icon="cloud" href="/cloud/verification/cloud-api/image-provenance">
    Use this when your application sends requests through `cloud-api.near.ai`.
  </Card>
  <Card title="Direct endpoint image provenance" icon="server" href="/cloud/verification/direct/image-provenance">
    Use this when your application sends requests directly to a model's `completions.near.ai` endpoint.
  </Card>
</CardGroup>
