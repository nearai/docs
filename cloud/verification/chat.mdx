---
title: "Choose a Response-Signature Flow"
description: "Select the response-signature guide that matches the endpoint your application calls."
---

The signature payload and the attestation to match depend on the request path.

<CardGroup cols={2}>
  <Card title="Cloud API response signatures" icon="cloud" href="/cloud/verification/cloud-api/response-signatures">
    Use `signature_kind` when present to select a gateway or model attestation.
  </Card>
  <Card title="Direct endpoint response signatures" icon="server" href="/cloud/verification/direct/response-signatures">
    Match the signature to the direct model report.
  </Card>
</CardGroup>
