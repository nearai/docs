---
title: "Choose a Model Attestation Flow"
description: "Select the verification guide that matches the endpoint your application calls."
---

The report structure depends on the endpoint, so select the guide for the request path your application uses.

<CardGroup cols={2}>
  <Card title="Cloud API model attestations" icon="cloud" href="/cloud/verification/cloud-api/model-attestations">
    Use `model_attestations[]` returned by cloud-api.near.ai.
  </Card>
  <Card title="Direct model attestation" icon="server" href="/cloud/verification/direct/model-attestation">
    Use the model report returned by a direct completions endpoint.
  </Card>
</CardGroup>
